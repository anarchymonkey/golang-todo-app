

/*

    Initial config should be of a type of

    {
        path: string,
        component: React.ReactNode,
    }

*/

const useRoutes = ({
    initialConfig
}) => {

    const format = () => {
        
    }

    // return a getter function which formats the route internally and gets the routes
    return 
}